import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.melaine.gerard.weather',
  appName: 'weather-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
